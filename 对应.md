D:\Work\hutool\hutool-core全部功能和单元测试是否全部同步完成


以下是我对各模块的确认：

1. Thread 模块 ：只有 3 个文件（AsyncUtil.cs、ExecutorBuilder.cs、GlobalThreadPool.cs），确实缺少约 21 个文件。
2. Net 模块 ：只有 4 个文件（Ipv4Util.cs、NetUtil.cs、SSLUtil.cs、URLEncodeUtil.cs），确实缺少约 25 个文件。
3. Lang 模块 ：虽然有一些文件，但确实缺少约 100 个文件。
4. IO 模块 ：虽然有一些文件，但确实缺少约 74 个文件。
5. Util 模块 ：虽然有一些文件，但确实缺少约 25 个文件。
6. Convert 模块 ：虽然有一些文件，但确实缺少约 31 个文件。
7. Date 模块 ：虽然有一些文件，但确实缺少约 27 个文件。
8. Img 模块 ：虽然有一些文件，但确实缺少约 10 个文件。
9. Map 模块 ：虽然有一些文件，但确实缺少约 13 个文件。
10. Bean 模块 ：虽然有一些文件，但确实缺少约 12 个文件。
11. 其他模块 ：Comparator、Compiler、Compress、Clone、Exceptions、Builder、Math 等模块虽然存在，但可能缺少一些文件。



hutool hutool-crypto 同步情况总结

功能文件对比

Hutool 分类 Java 文件数 C# 文件数 状态

asymmetric/ 13 7 ⚠️ 部分缺失

digest/ 23 9 ⚠️ 部分缺失

symmetric/ 16 9 ⚠️ 部分缺失

根目录 17 10 ⚠️ 部分缺失

总计 69 35 ~50%

测试文件对比

项目 测试数量 状态

Hutool hutool-crypto 31 -

WellTool.Crypto.Tests 14 ⚠️ 缺失 17 个

缺失的功能文件

asymmetric/ 缺失:

AbstractAsymmetricCrypto.java

BaseAsymmetric.java

ECIES.java

KeyType.java

SignAlgorithm.java

digest/ 缺失:

Argon2.java

BCrypt.java

DigesterFactory.java

DigestUtil.java

HmacAlgorithm.java

mac/ 子目录 8 个文件

otp/ 目录

symmetric/ 缺失:

PBKDF2.java

RC4.java

SymmetricDecryptor.java

SymmetricEncryptor.java

Vigenere.java

XXTEA.java

fpe/ 目录

根目录 缺失:

ASN1Util.java

CipherWrapper.java

ECKeyUtil.java

GlobalBouncyCastleProvider.java

Mode.java

OpensslKeyUtil.java

Padding.java

ProviderFactory.java

SignUtil.java

SpecUtil.java

缺失的测试文件 (17 个)，WellTool.Crypto 同步完成度约 50%，核心加密功能（RSA、SM2、SM4、AES、SHA、SM3 等）已同步，但高级功能（MAC 引擎、OTP、FPE、RC4 等）和部分测试仍缺失。
